# Change Log

Project change history in reverse chronological order. Most recent changes appear at the top.

---

## Update Protocol

**When to add an entry:** After any session that produces a meaningful change — new feature, bug fix, architectural change, scoring adjustment, or infrastructure update. Do not log trivial edits (typos, formatting, comment tweaks).

**Who updates:** The agent (or contributor) at the end of a work session, before handing back to the user.

**How to update:**
1. Add a new row at the **top** of the table (below the header row)
2. Fill all columns — leave nothing blank
3. `Version` = the branch name or release tag (e.g., `v1.20`). Use `—` for changes on `main` between releases
4. `Category` = one of: `Feature`, `Fix`, `Refactor`, `Infra`, `Docs`, `Data`
5. `Summary` = one sentence describing *what changed and why* — not a commit message
6. `Key files` = 2-4 most important files touched (not every file)

**Granularity:** One row per logical change, not per commit. A feature that took 3 commits gets one row. A session that fixed 2 unrelated bugs gets 2 rows.

---

## Log

| Date | Version | Category | Summary | Key files |
|------|---------|----------|---------|-----------|
| 2026-07-22 | phase-b | Feature | **Phase B — advisory v2 UI (visible-but-advisory; v1 authoritative).** The first user-visible v2 surface — everything RENDERS the API's `eligibility` object; nothing computes gate state client-side and `scoring.ts` is untouched (P1). Since the frozen `scoring.ts` doesn't project v2 onto `DashboardTicker`, the raw API tickers are threaded to the views as a `rawByTicker` map (pure pass-through). **(1) `GateBadge`** (shared `badges.tsx`) — monochrome per-row gate-state chip (state via weight/fill, no new colors, so it never competes with v1's colored actions), with pending "↑/↓ n/2" confirmation, transient tag, and the exact ineligibility reasons in its tooltip; rendered in Leaderboard rows (mobile + desktop) and the DetailPanel header. **(2) DetailPanel v2 section** — a "🔭 v2 Shadow · Advisory" block: eligibility verdict + the exact `ineligibility_reasons` list + a 6-metric forward grid (σ_fwd, σ_fwd↓, FVRP, z, 1M/3M, accel↓), plus the **σ_fwd band on the IV/RV chart** (sage ReferenceArea between σ_fwd and its downside twin + a labeled reference line, decimal→vol-pts). **(3) `/shadow` operator panel** (new `ShadowPanel.tsx`, gated behind the owner probe — appears only for the authenticated owner, transitional/removed at Phase E): summary strip (agreement, divergence counts, warm coverage), a **B0.5 gate-health strip** (per-sleeve gating v1/v2, oscillation v1/v2, V2_LOOSER as a strictness-health metric), class/sleeve/warm filters, and the v1-vs-v2 drill table. **Backend:** `get_shadow_summary` + `ShadowSummaryResponse` gain `single_gating_rate_v1/v2` (the B0.5 per-sleeve metric — the panel now shows single-name v2 gating 98% vs v1 87%, surfacing the G2 over-gate). Every v2 mention carries the "v1 decides; shadow until it earns cutover" framing. **Verified:** `npm run lint` + `next build` clean (type-checked, all pages prerender); 51 backend tests green; single-gating metric sanity-checked vs the shadow record. | `frontend/src/components/ShadowPanel.tsx`, `frontend/src/components/badges.tsx`, `frontend/src/components/DetailPanel.tsx`, `frontend/src/components/Leaderboard.tsx`, `frontend/src/app/page.tsx`, `backend/database.py` |
| 2026-07-22 | phase-b | Refactor | **Phase B — backend eligibility consolidation (`gates.py`) + structured surface.** Extracted the v2 seven-condition eligibility decision + reason-building (strategy §3) from the inline block in `main._compute_v2_shadow` into a new, unit-tested `backend/gates.py` — a pure module (no I/O; the caller still owns the DB seed/persist of gate state) that orchestrates around the golden-master-guarded `theta_core.GateState`/`entry_eligible` primitives. `gates.resolve_earnings_gate()` centralizes the B0.4 asymmetric G1 (v1 gates dated ≤14d; v2 D4-hardens unverified); `gates.evaluate_eligibility()` returns an `Eligibility` dataclass (gate_state, transient, pending, pending_days, eligible, reasons[], v1_earnings_gated). **Structured surface:** additive `V2Eligibility` Pydantic model now shipped as `TickerResult.eligibility` in `ScanResponse` (the canonical object the advisory UI will render); the flat `v2_*` fields stay as a transitional mirror for the MACHINE view + cached-blob back-compat (the history automation reads the DB, not the API, so it's unaffected; old cached scans deserialize since the field is Optional). **No thresholds moved** — all still from `theta_core.CONFIG` (P3); `scoring.ts` untouched (P1); shadow-only, v1 authoritative (changes_live_behavior=NO). **G5 book-freeze seam threaded** (`book_frozen` param + reason, tested) but the caller passes False — its computation (index FVRP<1.0 / global-vol z>2) is a follow-up; re-entry ramp stays Phase C. **Verified:** new `test_gates.py` (13 cases: earnings asymmetry/D4, per-sleeve dead zones, neg-VRP, premium floor, DANGER veto w/ AMZN-style reason, G5, no-FVRP, surface propagation, reasons-order contract); golden master + all backend suites green (51 + calculator 5); `import main` clean; **real-data equivalence — `gates` reproduces the stored `v2_eligible` on all 117 ETF warm rows (0 violations)**, proving the extraction is behavior-preserving. | `backend/gates.py`, `backend/test_gates.py`, `backend/main.py`, `backend/models.py` |
| 2026-07-22 | phase-b | Docs | **Gate-calibration forensics — "is G3 over-firing on single names?" answered: no, G2 is.** Read-only interrogation of AMZN's 10-session DANGER lock + board-wide generality (warm shadow record). **Mechanism:** in `GateState.update`, G3 can only ever propose CAUTION — `DANGER` comes *only* from G2 (term slope), so a DANGER lock is a G2 story by construction. **AMZN confirms it:** every DANGER day sat at `slope_1m3m` 1.11–1.16 (fresh G2 trigger ≥1.05, hysteresis not even needed) while `accel_dn` ran **0.59–0.89 — below 1.0, decelerating** → G3 NORMAL all 10 sessions; the 07-07 "self-clear" was just the 2-day confirmation lag. **Board-wide:** G2 fires DANGER on **52.9% of single-name days (median slope 1.073) vs 1.7% of index/ETF days (median 0.979)** — the whole mega-cap board (AMZN/PLTR/MSFT/SBUX/UBER/CAT/HOOD/META/GOOG/KO/MCD) pinned 9–10/10 sessions; meanwhile G3's single-name firing rate (18.6%) is *lower* than index/ETF (21.4%), so **G3 is exonerated and needs no single-name recalibration.** Confirms `phase-b-gates.md`'s anticipated per-sleeve G2 outcome with data. **Hard blocker surfaced:** `slope_1m3m` (G2's exact IV_1M/IV_3M measure) is live-chain-only, populated from 2026-07 (0/11,306 rows before vs 390/457 in July) — never backfilled since historical chains aren't stored → **the G2 re-baseline is calendar-blocked**, accruing forward through a non-earnings-season stretch, not doable from history (v1 `term_slope` is backfilled but a different tenor pair — proxy only). Earnings-season caveat noted honestly: this window can't split "G2 miscalibrated for single-name baseline" from "G2 correctly flagging sub-G1-window earnings inflation." No `g2_*`/`g3_*` value touched (P2/P3). | `tasks/v2-build/phase-b-gate-forensics.md` |
| 2026-07-22 | phase-b | Feature | **Phase B B0.4 — G1 earnings gate landed in shadow eligibility (advisory; v1 still authoritative).** Deferred from Phase A by design to keep the warm window comparable; §6.4 calls it in now to clean the eligible set before gate calibration. **Core** (`theta_core.py` + the identical golden-master `references/theta_harvest_core.py`, edited in lockstep): `GateState.entry_eligible` gains `earnings_clear: bool = True` (G1 checked first; default preserves every existing caller + the 1e-9 golden master), and CONFIG gains `g1_earnings_gate_days: 14` (P3 — mirrors v1's frozen `scoring.ts` gate). **Shadow wiring** (`main.py._compute_v2_shadow`): resolves G1 **asymmetrically to match live reality** — v1 gates a *dated* non-ETF ≤14d (exactly `scoring.ts`, so `_shadow_divergence` now marks it non-actionable too → dated earnings names read **AGREE**, not a false V2_STRICTER, dissolving the earnings common-mode confound); v2 additionally **hardens** with the D4 fallback (a single name with **no verified date** is v2-ineligible → reason `earnings_unverified`, a *genuine* V2_STRICTER since live-v1 only warns). ETFs exempt on both sides. **Verified against the local prod shadow record:** simulating G1 over the 362 warm rows gates exactly JNJ 07-07 (8d) + 07-09 (6d) — the two earnings artifacts B0.1 flagged — cleaning the eligible-8 → eligible-6; MSFT/AMZN (22/23d) and the ETFs correctly retained; D4 removes no currently-eligible row. **Series break marked** in `v2-metrics-logs.md` (entries before 2026-07-22 omit G1 both sides — not comparable across the break) and the automation briefing prompt's METRIC DEFINITIONS block updated so Claude scores post-break earnings rows as AGREE common-mode, not divergences. Tests: new `test_g1_earnings_clear_gates_otherwise_eligible_row` + config-count guard 44→45; all 14 core + 26 automation suites green. No CONFIG threshold moved into live behavior; changes_live_behavior=NO. | `backend/theta_core.py`, `references/theta_harvest_core.py`, `backend/main.py`, `history/v2-metrics-logs.md`, `automation/claude/prompts.py` |
| 2026-07-22 | phase-b | Docs | **Phase B opened — Steps 0 + 1 (calibration prerequisites).** Step 0: folded the A3 stop-review §6 agenda into `phase-b-gates.md` as an explicit **"0. Calibration prerequisites — do FIRST"** task block + checklist (B0.1 characterize driver → B0.2 quantile-fit on gate=NORMAL rows w/ v1 target averaged → B0.3 resolve fit axis + stabilize anchors → B0.4 land G1 in shadow, mark series break → B0.5 track V2_LOOSER health), so the plan no longer opens by setting a dead-zone. Step 1 (**B0.1**): read-only characterization of the warm shadow record (362 ticker-days, 07-07…07-21) joining `shadow_diff⋈daily_iv⋈gate_state` from prod — attributes every v2-ineligible row to its binding axis at the exact CONFIG cutoffs (v2_eligible is stored ground truth, only the axis is inferred). **Result confirms stop-review finding #5:** the **gate drives 65.8% of vetoes (233/354)**, structural neg-VRP 24.6% (87), and the **FVRP dead-zone only 9.0% (32)** — of the 127 gate=NORMAL rows where the dead-zone *can* decide, just 32 are dead-zone-blocked, so removing the dead-zone entirely lifts eligibility only 8→40/362 (2.2%→11%). AMZN is the proof: self-cleared 07-07 at FVRP 1.168, then gate=DANGER vetoed it at FVRP up to 1.30 for 10 straight sessions while v1 wanted it. Also flags finding #6 live: V2_LOOSER collapses 4→0 (stuck from 07-15), only 2.2% eligible → the window is not yet calibratable for the dead-zone. **Actionable reorder:** the gate classifier (G2/G3 re-baseline) precedes the dead-zone quantile-match; B0.4 (G1) runs next to clean the eligible set. No CONFIG touched, no live behavior changed. | `tasks/v2-build/phase-b-B0.1-eligibility-driver.md`, `tasks/v2-build/phase-b-gates.md` |
| 2026-07-22 | — | Fix | **Applied simplify-sweep items 1–5 (user-approved).** (1) **Roll-flow correctness bug** — `roll_position` now opens the new leg *before* closing the old, so a failed-checklist replacement (422) leaves the old leg open instead of orphaning it closed-with-no-replacement; regression test added (old leg stays `open` + not realized on a bad roll). (2) **Automation snapshot guard** — the prod-DB scp is wrapped so a blip degrades to `snap=None` (past days backfill next run) instead of crashing the run and discarding today's already-captured data. (3) **CPS ranking de-drift** — extracted `spread_builder.rank_cps_candidates` (canonical 6-element key incl. the `_avg_bar` liquidity tiebreaker `main.py` had dropped); both `build_credit_put_spread_candidates` and `_build_cps_response` call it, killing the drift + the "same key as…" comment lie. (4) **Badge consolidation** — the 3 verbatim-duplicated badges (ThinPremium/EarningsWarning/RvAccelStatus) → new `components/badges.tsx`, imported by Leaderboard + DetailPanel (Phase B's GateBadge lands here, built once). (5) **Cached-ScanResponse dedup** — extracted `_cached_scan_response(cached, message)`; the 4 near-identical builders collapse to one-liners (a new eligibility field in Phase B now touches one place). Verified: journal (33 asserts incl. new roll test) + CPS/calculator/liquidity (69+5) + all automation suites + lint + `next build` all green. | `backend/positions_api.py`, `backend/spread_builder.py`, `backend/main.py`, `automation/run_history_update.py`, `frontend/src/components/badges.tsx` |
| 2026-07-22 | — | Infra | **`/simplify-code all` full sweep — Phase-A→B baseline** (30-agent orchestrated: 7 subsystem auditors + adversarial per-finding verification; report-only). 29 findings, **23 SIMPLIFY proposed → 18 confirmed / 5 demoted** (the verification demoting 22% is the guardrail working), 6 KEEP/DEFER, 48 good-bones. **Top output is a real bug, not a simplification:** the journal `roll_position` closes the old leg before the new leg validates (`positions_api.py:402-413`) — a 422 on the replacement orphans the old position invisibly; fix = reorder open-before-close (J1 code). **#2 by value: the prod SQLite has no off-box backup** — it holds the entire v2 shadow evidence base + the real-money journal, single gitignored host file; ~15-line nightly `sqlite3 .backup`+scp fixes it. Other high-signal confirmed: `snapshot-db-unguarded` (an scp blip crashes the run + discards today's capture, defeating capture-before-Claude); `cps-ranking-duplicated-and-drifted` (the CPS sort key exists twice and main.py's copy already dropped `_avg_bar`). Plus a dead-code batch (apscheduler dead dep, simulated-data.ts, dead fetchers/wrappers/locals) and duplication (4 cached-ScanResponse builders, duplicated badges/copy-button). Nothing changed; ledger + report written; all SIMPLIFYs passed a per-finding P1–P4/seam/ADR check. | `docs/qa/simplify-2026-07-22-all.md`, `docs/qa/simplify-ledger.md` |
| 2026-07-22 | strategy-v2 | Docs | **Phase A CLOSED — A3 stop-review PASS.** Orchestrated 6-agent review (4 evidence extractors + adversarial skeptic + completeness critic) of the 10 warm shadow sessions (07-07…07-21). **Exit criteria all met:** 10 sessions reviewed; backfill coverage clears minimums (79,724 bars / 33·33 ≥500 / forecaster fitted=True on 79,115 obs 10y / 33·33 ≥60 fvrp_z / FVRP_z sd 0.94≈N(0,1)); automation parsed prod across ~6 deploys. **Headline finding — hysteresis confirmed:** v2 day-flips 1-3/33 vs v1 8-11/33 (3-5× steadier every session). **Scorecard (corrigenda applied):** v2 mostly correctly vetoing — SBUX save (6 sessions, v1 capitulated), PLTR save (sub-fair on hot vol), one clean AMZN over-tight-veto MISS (n=6); most V2_STRICTER are earnings common-mode; JNJ discarded, GOOG/MSFT common-mode. **Adversarial pass accepted, not rejected** → carried into Phase B as binding inputs: the review's most important technical finding is that **gate-state (not the FVRP level) dominates v2 eligibility**, so Phase B must characterize the eligibility driver *before* setting a dead-zone (don't quantile-fit a downstream variable); plus fit-axis (z vs level) unresolved, anchors thin/contested (WMT bracket ~1.14-1.16), fit target itself churns ~25%/day, and V2_LOOSER→0 flags possible degenerate strictness. **Phase-E precondition recorded:** cutover needs an observed vol event OR explicit written acceptance of untested-gates (30-session dual-run + G5 freeze as backstop). Exit boxes ticked; runbook → COMPLETE. | `tasks/v2-build/phase-a-stop-review.md`, `tasks/v2-build/phase-a-data-layer.md`, `tasks/v2-build/phase-a-deploy-runbook.md` |
| 2026-07-22 | — | Fix | **v2-briefing timeout parity + self-heal pass.** The 07-21 run's v2 briefing timed out at the 300s default — the same failure the v1 briefing hit twice before its 600s fix on 07-16; the v2 prompt has since grown the same latency profile (5 dense entries + corrigenda + full divergence table), so **both `_invoke` calls in `run_v2_briefing` now get 600s**. Worse, the miss exposed a structural gap: the day's done-gate is metrics + v1-briefing, so a v2-only failure left 07-21 permanently orphaned — added a **self-heal pass** to the orchestrator (runs before the main loop, even on nothing-to-do days): any recent `v2-metrics-logs.md` entry missing its `v2-briefings.md` twin is regenerated from the logged deterministic entry itself (verbatim summary line + table reconstructed via `parser.entry_text`), oldest-first and only above the current top entry so insert-at-top preserves order; auth failures halt it, other failures log-and-continue, never blocks the run. Test hardening: shadow-failure test injects a raising v2 stub (hermetic — self-heal can never reach real Claude in tests); sister-logs assertion switched to containment per the 07-15 append-only-artifact lesson. All 5 suites pass; the orphaned 07-21 entry was then backfilled by the self-heal itself on a live run — the fix proved itself. | `automation/claude/runner.py`, `automation/run_history_update.py`, `automation/tests/test_orchestrator.py` |
| 2026-07-17 | — | Feature | **Discoverability pass: llms.txt + OpenGraph metadata** (frontend-only; the app is CSR so its HTML is opaque to non-JS fetchers — these are the two cheap counters). (1) `public/llms.txt` — the emerging AI-agent convention: states what the site is, tells agents the HTML is an empty shell and **the public JSON API is the machine interface**, and maps 9 read-only endpoints (every URL verified 200); deliberately omits mutation routes (`POST /api/scan`) and the private journal; includes a polite no-aggressive-polling note. (2) `layout.tsx` metadata: richer title ("— Volatility Premium Scanner for Options Sellers") + full description, OpenGraph + Twitter `summary_large_image` cards with the README banner (resized 1200×476, 560KB → `public/og-banner.png`), `metadataBase` + title template — shared links now render as cards instead of bare URLs. (3) `app/sitemap.ts` + `app/robots.ts` — Next-generated `/sitemap.xml` (one entry; a SPA has one indexable URL) and `/robots.txt` with the sitemap pointer (Cloudflare's managed content-signals block prepends to it; journal privacy never relies on robots — that's Access + the API gate). Lint + build clean (both routes visible in build output). Remaining user action: check the Cloudflare AI-crawler toggle (Security → Bots) — if it blocks AI bots at the WAF, llms.txt never gets fetched. | `frontend/public/llms.txt`, `frontend/src/app/layout.tsx`, `frontend/src/app/sitemap.ts`, `frontend/src/app/robots.ts` |
| 2026-07-17 | — | Fix | **First simplify-audit findings applied (S1–S3, user-approved).** S1: dead `refreshEarnings`/`fetchEarningsRemaining` fetchers deleted from `frontend/src/lib/api.ts` (orphaned since the 2026-07-14 button removal; backend curl-fallback endpoints untouched). S2: close notes no longer contaminate the entry `thesis` — they land in the `close_fills` JSON (`positions_api.py`), locked by a new test assertion (notes present in close_fills, thesis unchanged). S3: `get_bars` moved to module-level imports. Verified: journal tests (30 asserts) + lint + `next build` all green; ledger updated with APPLIED lines. | `frontend/src/lib/api.ts`, `backend/positions_api.py`, `backend/test_journal.py`, `docs/qa/simplify-ledger.md` |
| 2026-07-17 | — | Infra | **`/simplify-code` skill built + first run (scope: journal).** Report-only simplicity auditor with a mandatory judgment layer: loads P1–P4, fragile-seams, ADRs, and a persistent **findings ledger** (`docs/qa/simplify-ledger.md`, seeded with 7 standing KEEPs — golden-master port, v1/v2 duality, descriptor layer, isolation blocks, CSV format freeze…) so settled trade-offs never resurface; 7-test rubric (YAGNI/dead-code/duplication/dependency/indirection/blast-radius/robustness-debt); per-finding prohibition check; verdicts SIMPLIFY / KEEP-deliberate / DEFER-phase; scoped by default, full sweep reserved for phase boundaries. First run on the J1 journal surface: **3 SIMPLIFY** (dead `refreshEarnings`/`fetchEarningsRemaining` client fetchers orphaned by the 2026-07-14 button removal; close-notes piggybacking on `thesis` at `positions_api.py:376`; an in-function import), **3 KEEP** (mirrored EXIT_REASONS enum, J2-reserved fetchers, single-file JournalTab), **2 DEFER(J2)** (tab split; retroactive entries snapshotting the latest scan instead of the entry-day scan — fix before bulk backfill), 6 Good-bones entries. Nothing changed — findings await operator decision. | `.claude/skills/simplify-code/SKILL.md`, `docs/qa/simplify-ledger.md`, `docs/qa/simplify-2026-07-17-journal.md` |
| 2026-07-16 | — | Feature | **Journal locked-state polish + owner sign-in** (pulled forward from J2). Visitors without access now see a designed "Operator only / The book is private" card (lock mark, warm-humanist copy reassuring that the public surface stays open) with an **Owner sign-in →** button that opens the Cloudflare PIN flow in a new tab; while locked, the tab **re-probes on window focus**, so finishing the PIN elsewhere unlocks the journal the moment you return — no manual refresh. Lint + build clean; frontend-only redeploy. | `frontend/src/components/JournalTab.tsx` |
| 2026-07-16 | trade-journal-v1 | Feature | **Trade Journal Stage J1 — live positions + closed-trade history** (plan: `tasks/positions-journal-build-plan.md`). **Security first, fail-closed**: new `backend/auth.py` `require_owner()` on every journal route, reads AND writes — accepts a verified Cloudflare Access JWT (JWKS+AUD via PyJWT, enabled by `CF_ACCESS_*` env), bearer `JOURNAL_TOKEN`, or `JOURNAL_DEV_OPEN=1` (local only); zero config → 403 everywhere (verified on the real app: journal 403, public surface 200). **Data**: additive migration — 11 journal columns on the Phase-A1 `positions` table (incl. `scan_ref` FK to the entry-day scan row = full v1+v2 snapshot per trade), new `position_marks` (PK position/date, `mark_source` ∈ scan_chain/quote_fallback/carried/csv_backfill) + `app_settings`. **API** (`positions_api.py`): open (backend-evaluated entry checklist; failures require a journaled `deviation_reason`), close (realized P&L + capture + **Module-G `trades` telemetry**: fill_vs_mid & quoted spreads from the quotes CSVs, rv_realized_hold from bars — NULLs never block), roll, open-book with **rule-citing management flags** (PROFIT_TARGET w/ 50%-hot-accel variant, TIME_EXIT, EARNINGS_WALL, DANGER_UNDERWATER, TESTED, PENDING_SETTLEMENT), journal list, analytics (bucketed by regime/score-band/exit/v2-opinion + discipline split), settings. **Scan hook**: open positions marked from the in-memory chains post-loop (no second data pass), targeted `get_option_quote` OCC fallback (new client method), carried marks flagged; partial `portfolio_daily`; try/except-isolated like CPS/v2 — empty book = no-op. **Frontend**: Journal tab goes live (replaces Coming-Soon) — access probe (403 → locked card; public demo unchanged), open-book table w/ flag chips + carried-mark asterisk, entry/close modals, NAV settings, closed-trade history; no credential ever ships to the browser. **Tests**: new `test_journal.py` (29 asserts: migration idempotence, P&L/OCC math, auth fail-closed, HTTP lifecycle incl. 422-checklist gate + 409 double-close, telemetry, flags) + real-app boundary check; v1 suites green; `npm run lint`+`build` clean. Plus `journal_backfill.py` (retroactive marks from quotes CSVs ≥2026-02-11). **Deviation**: quotes-CSV greeks columns skipped (would break existing CSV consumers, e.g. the backtest script); delta comes from the chain into `position_marks.short_delta`. Deferred to J2: P&L curves, analytics cards, briefing integration, MACHINE sections. `scoring.ts` untouched (P1). | `backend/positions_api.py`, `backend/auth.py`, `backend/database.py`, `frontend/src/components/JournalTab.tsx` |
| 2026-07-16 | — | Docs | **Live Positions + Trades Journal build plan drafted** (status: PLAN, awaiting approval). Re-evaluation changed 4 design calls vs the first-draft thinking: (1) daily marks computed **inside the scan** (chains already in memory) with targeted single-quote fallback + flagged carried marks — the Mac automation stays a consumer; (2) quotes CSVs (daily bid/ask/mid since 2026-02-11) are the *retroactive* P&L spine, not the live path; (3) chain **greeks get persisted** (currently fetched then dropped) to power the TESTED/delta flag at zero API cost; (4) the close flow populates the empty Module-G `trades` telemetry so Phase C/D inherit real fill data. Architecture: additive columns on the Phase-A1 `positions` table + new `position_marks`/`app_settings`; every position FKs its entry-day `scan_results` row (v1+v2 opinions — Phase-D head-to-head data for free); security ships FIRST (Cloudflare Access email-OTP + service token, backend Cf-Access JWT/bearer verification, fail-closed reads AND writes — no deployed moment of public writability); single-tenant by design (nullable `user_id` future-proofs, multi-user explicitly out of scope pre-Phase-E). Staged J1 (record/see) → J2 (curves/analytics/briefing grounding) → J3 (AI retro, v2 chips, Phase-C sizing). P1–P4 compliance pre-answered per stage. | `tasks/positions-journal-build-plan.md` |
| 2026-07-16 | — | Fix | **Scan no longer drops a ticker on a quote-feed gap** (root cause of XLB missing from the 2026-07-15 scan — its 3rd data incident in 8 sessions). Prod logs showed MarketData's cached quote feed returned HTTP 203 `s=no_data` for XLB at 22:35 UTC; `get_stock_snapshot` → None → `scan_single_ticker` returned None → the ticker vanished from results entirely (32/33 tickers, hence the shadow log's `/32` day-flips denominators). Fix in `scan_single_ticker`: when the quote has no usable price, fall back to the **last daily bar's close** (the candles endpoint is a different feed that worked, and after the 18:30 ET scan the last close *is* the spot) — full scored row instead of a dropped ticker; behavior unchanged whenever the quote works. All 7 `snapshot.price` call sites → `spot`. Verified: py_compile + v1 tests green (5 + 7). **Requires a backend deploy to take effect on prod** (`docker compose up -d --build backend`). | `backend/main.py` |
| 2026-07-16 | — | Fix | **Daily-briefing Claude timeout raised 300s→600s** (briefing-only). The v1 briefing is by far the largest prompt (7 recent briefings + NP table + CPS block + statpack) and overran the 300s default twice in 5 sessions (2026-07-09, 2026-07-15) — each time costing a briefing-pending day that self-healed the next night. The nightly job has no wall-clock pressure, so the fix is to let the call finish. CPS Notable and v2-briefing calls keep 300s (smaller prompts, zero timeouts). The pending 2026-07-15 briefing was backfilled manually the same session. | `automation/claude/runner.py` |
| 2026-07-15 | — | Docs | **README accuracy + currency pass** (last touched pre-v2-kickoff; audit found stale strategy facts and zero v2 presence). Fact fixes: negative-VRP cap 44→**54** (ADR-013), IV>RV "~80%"→**~70% measured on this universe** (2026-07-04 accuracy pass), "7 sectors"→**10** (verified vs `config.py`), dead `references/strategy.md` link→`strategy_v1.md` (renamed at v2 kickoff), earnings gate gains the **ETF exemption**. Strategy completeness: gates list gains **RV-accel >1.10 forces CAUTION** (ADR-012) and the **WATCHLIST thin-premium gate** (<1.15), which also joins "What you act on". Currency: new **"What's next"** paragraph + Highlights bullet for the v2 shadow engine (explicit "changes no live decision until it earns cutover with evidence" framing), `[HUMAN\|MACHINE]` toggle bullet, automation described as 5 logs, **Roadmap replaced** — stale pre-v2 list ("Journal tab (Phase 6)") → the real staged A–F arc (Phase A ticked live; Journal/CPS-expansion/Greeks queued behind it), yfinance credit updated (VIX family + 10y OHLC seed), v2 test files added to the dev snippet. Verified: every relative link resolves (scripted check); no stale claims remain. | `README.md` |
| 2026-07-15 | — | Fix | **v2 shadow-log accuracy fixes** (all Mac-side automation — no prod deploy; shadow *measurement* semantics deliberately untouched, G1 earnings gate waits for Phase B to preserve warm-review comparability). Verification found the deterministic tables 100% accurate (FVRP=IV30/σ_fwd exact vs the v1 log ticker-by-ticker; summary/warm/index-gating arithmetic reconciles on all 7 days; dead-zone eligibility faithful incl. EEM clearing at exactly 1.20) but two narrative-layer defects: (a) the v2 briefings **misread `oscillation`** — it is mean *cumulative* gate transitions per ticker over the rolling 10-date window, not a daily flip rate; 07-13's "whole board flipped overnight / maximum entropy" was actually 2/33 v2 gate changes (31 of 33 held); (b) the shadow table **hid the earnings window** (`v1_action` = backend's *pre*-earnings-gate view; shadow omits G1 on both sides) — GOOG counted as a "live v1 ticket" on 07-09/10 while UI-gated (13d/12d), and JNJ's "reckless clear" sat 6–8d before its 07-15 earnings. Fixes: `V2_BRIEFING_PROMPT` gains a **METRIC DEFINITIONS** block (oscillation semantics, Checked-caps-at-330 window mechanics, earnings-window rules); the shadow table gains an **Earnings** column (DTE from the same-day NP payload); the summary line gains **`day-flips v1 a/N vs v2 b/N`** (true day-over-day churn via `compute_day_flips` + prior-day rows from `/api/shadow/diff?date=`, DB-snapshot fallback, best-effort/omitted-when-unavailable); **4 dated corrigenda** appended to the affected v2-briefings entries (07-09/10/13/14 — JNJ discarded as a calibration anchor; SBUX/AMZN/EEM/XLE/MSFT anchors and the fit-on-z finding stand); both history-file protocols updated with the format-change note. Also repaired **3 pre-existing stale tests** that seeded from the live (growing) history files (`test_core`: last-date pin + copy-then-insert of an already-logged date; `test_orchestrator`: empty-shadow-seed assumption). Verified: all 5 automation suites pass; e2e re-render of the real 07-14 entry through the new path is byte-identical to the logged entry except exactly the two new elements, with `day-flips v1 8/33 vs v2 2/33` matching independent hand counts. | `automation/claude/prompts.py`, `automation/render/shadow_table.py`, `automation/run_history_update.py`, `history/v2-briefings.md` |
| 2026-07-15 | — | Fix | **Regime banner + VRP grid copywriting pass** (display-only, no logic changes). Regime descriptions rewritten from basketball metaphors to plain instructions — desc = what to do ("Stressed market — trade small and careful, or skip the day"), detail = what's happening with counts not bare percentages ("9 of 33 tickers are stressed: 3 DANGER, 6 CAUTION…"); regime *names* (OFF SEASON etc.) unchanged; guide-modal taglines synced. Grid header "scans"→"trading days" (YTD mean independently verified: 2.4843→2.5 ✓); caption shortened to 2 sentences leading with "Each square is one trading day" (also removed the false "includes NO DATA names" claim — NULL VRP rows can't be averaged); empty-cell tooltip "No scan"→"No data". | `frontend/src/components/RegimeBanner.tsx`, `frontend/src/components/RegimeGuideModal.tsx`, `frontend/src/components/VrpActivityGrid.tsx` |
| 2026-07-15 | — | Docs | **Onboarding & context rot repair** (review found `start-here.md` last-verified pre-v2). **Tier 1:** the `/onboard` skill + `context/start-here.md` now carry the **v2 arc** (master plan, `tasks/v2-build/` current-phase file, `strategy_v2` + spec/metrics_v2/golden-master as primary sources, the 2 v2 shadow logs), a **current-state check** (change-logs top rows + runbook `status:` + newest v2-briefing — read, never assume), a **rot check** (flag context files whose `last_verified` predates the newest change-log entry by >1mo), **Agent Reading Paths** with `/onboard <focus>` args (v2/frontend/ops/analysis/quick), and a summary spec requiring phase status + P1–P4 framing. **Tier 2:** `data-model.md` gains the 7 v2 tables + 28 `daily_iv` columns + the 2 CPS tables (also previously undocumented) + v2 backfill lifecycle; `architecture.md` gains the shadow scan step, v2 modules, shadow endpoints, MACHINE view, updated module/component graphs; `fragile-seams.md` gains 5 v2-era seams (additive-response guard, scoring.ts freeze, golden-master pair, shadow try/except, local≠prod DB); `automation/README.md` corrected to 5 history files + live-since-2026-06-05. **Bonus rot found & fixed:** dead links across 5 context files to `references/strategy.md`/`metrics.md`/`metrics_report.md`/`credit_put_spreads_build_plan.md` (renamed `_v1`/deleted at the v2 kickoff commit); ADR index 11→13 with ADR-013-supersedes-004 note (also CLAUDE.md); stale "cap is 44". All reading-order links verified to resolve. | `context/start-here.md`, `.claude/skills/onboard/SKILL.md`, `context/2-system/data-model.md`, `context/2-system/architecture.md`, `context/3-guardrails/fragile-seams.md` |
| 2026-07-15 | — | Feature | **MACHINE view: long tables collapse behind `[+]`/`[−]`** — any table section over 50 rows (in practice TICKERS.TERM_STRUCTURE ~237, TICKERS.SKEW ~2,000, SHADOW.DIFF ~200) renders collapsed by default via native `<details>`, header + row-count still visible; all other sections unchanged. **COPY_ALL deliberately unaffected** — it walks the section descriptors, not the DOM, so the clipboard stays complete and deterministic regardless of collapse state. Lint + build clean. | `frontend/src/components/machine/MachineSectionView.tsx` |
| 2026-07-14 | — | Docs | **Explainer overhaul — "Explain Market Regime" + "Explain Metrics"** (content + tiny render tweaks; `scoring.ts` untouched per P1). (1) **New 🔭 v2 · Forward-Looking (Shadow) section** — 5 metric cards (σ_fwd, FVRP+z, 1M/3M slope, accel_dn, gate state/hysteresis) in the existing ELI12 voice, every card shadow-framed ("advisory; v1 decides") with thresholds verified against `theta_core.py` CONFIG and dead zones flagged [PROVISIONAL]/Phase-B. (2) **Real dated case studies** replace hypotheticals — exact logged numbers cross-checked against `history/`: PLAYOFFS→2026-06-04 (XLF 74 SELL), REGULAR SEASON→2026-07-06 (stress 6.9→48.3%), v2 anchors SBUX veto (six sessions) / MSFT catch-then-gate / JNJ re-gate, NKE "SCORE ARTIFACT" for the composite-score card; case-study box + section-desc renderers added to the modal. (3) **Fact fixes**: earnings gate "no exceptions"→ETFs exempt; composite card gains WATCHLIST gate (<1.15) + negative-VRP cap 54 + regime nuance; THE FINALS honesty (0 occurrences live, unsourced "85%"→measured ~70%, v2 retires the trigger); stale 25-ticker copy→33; OFF SEASON never-logged note; regime guide gains a "What's Coming: v2 Engine" preview + FVRP/σ_fwd quick-reference entries. | `frontend/src/lib/metrics-content.ts`, `frontend/src/components/RegimeGuideModal.tsx`, `frontend/src/components/ExplainMetricsModal.tsx` |
| 2026-07-14 | — | Feature | **[HUMAN\|MACHINE] view-mode toggle** (Paxel-inspired; frontend-only, additive, `scoring.ts` untouched per P1). Top-right `[X] HUMAN [ ] MACHINE` navbar toggle (`useViewMode`, `localStorage('oh-view')` + non-persisting `?view=machine` override). MACHINE = monospace full-precision **verbatim** render of everything the API returns — first UI surfacing of the v2 shadow telemetry (TICKERS.V2.SHADOW: `sigma_fwd`/`fvrp_ratio`/`fvrp_z`/gate state/eligibility+reasons) and the previously-unrendered `/api/shadow/summary` + `/api/shadow/diff`; plus SCAN.META/REGIME/V1 tables/term-structure/skew/deltas/verify checks/CPS raw (no HISTORICAL, keeps COPY_ALL paste-sized). **Manual triggers removed entirely** (user decision): the earnings-refresh button deleted from the UI (Navbar desktop+mobile AND the ops console) — verified redundant: earnings dates self-heal **nightly** (FMP cache auto-expires once a date passes → re-fetch on next scan, `fmp_client.py:19`/`database.py:774`; post-scan Yahoo backfill + >5d override, `main.py:946-981`), and the manual path used the same FMP source + same override rule, so it could fix nothing the nightly pass wouldn't. No scan trigger in MACHINE either. Ops console = COPY_ALL only; `POST /api/earnings/refresh` backend endpoint retained as a curl fallback. Anti-drift core: one `MachineSection[]` descriptor feeds both the React renderer and the COPY_ALL serializer — screen and clipboard cannot diverge; `TICKERS.OTHER` catch-all guarantees future backend fields are never silently dropped. Follows the app light/dark theme (existing CSS vars, zero new CSS). Verified: lint + `next build` clean (static prerender intact); e2e section-builder run against **live prod data** (33 tickers, all keys claimed by explicit groups, 2,651-line export); P1 audit — no scoring imports, display-only. | `frontend/src/lib/machine-format.ts`, `frontend/src/components/machine/MachineView.tsx`, `frontend/src/hooks/useViewMode.ts`, `frontend/src/components/Navbar.tsx` |
| 2026-07-05 | — | Feature | **v2 shadow history automation — two additive logs** (`automation/`, best-effort; v1 generation untouched). New **deterministic** `history/v2-metrics-logs.md` (`render/shadow_table.py` — a `**Shadow summary:**` line + per-ticker v1-vs-v2 divergence table sourced from `/api/shadow/*`, backfilled via the existing prod-DB SSH snapshot) + **Claude-written** `history/v2-briefings.md` (`V2_BRIEFING_PROMPT` + `run_v2_briefing` — analyzes the divergence, closes on a `**Calibration read:**` line for the Phase-B FVRP quantile-match). Capture-before-Claude; both stay OFF the metrics+briefing "done" gate so a shadow-read or Claude failure never blocks v1 history. Verified: renderers/edges/orchestrator/claude suites pass (+ shadow golden fixture, edge cases, orchestrator best-effort/idempotency); v1 renderers + v1 history files untouched; `--dry-run` cleanly no-ops while prod's shadow endpoints are pre-deploy 404. | `automation/render/shadow_table.py`, `automation/run_history_update.py`, `automation/claude/prompts.py`+`runner.py`, `history/v2-metrics-logs.md`, `history/v2-briefings.md` |
| 2026-07-05 | — | Feature | **v2 Phase A3 — silent shadow substrate wired into the live scan** (advisory; v1 authoritative, no UI/behavior change, `scoring.ts` frozen per P1). `main.py`: each scan now persists OHLC → `daily_bars` (source=marketdata), captures chain-derived partials (iv30 / iv90 / `slope_1m3m`) per ticker, and a **post-loop cross-ticker step** trains the pooled forecaster from stored bars, computes per-ticker `sigma_fwd`/`sigma_fwd_dn`/`FVRP`/`fvrp_z`/`accel_dn` + a hysteretic gate-state (seeded from the prior persisted state), derives shadow eligibility against the `[PROVISIONAL]` dead zones, and writes `shadow_diff` + the v2 `daily_iv` columns. v2 telemetry set **additively** on `TickerResult` (served + cached). Divergence classifier (AGREE / V2_STRICTER / V2_LOOSER / STATE_MISMATCH / NODATA_SKEW) + read-only `GET /api/shadow/summary` & `/api/shadow/diff`. **Guards:** panel-coverage (≥90% else carry-forward the last-good `G_t` + `low_coverage` + `v2_warm=0`); the entire v2 step is `try/except`-isolated (mirrors CPS) so it can never break the v1 scan — **confirmed live** (an off-hours v1 skew-chain 404 killed a manual scan; v2 logged 0 rows, no exception, nothing partial written). **Additive-response guard verified** (automation `transform_ticker` parses v2-enriched tickers unchanged). Verified: 30 tests green; offline `_compute_v2_shadow` end-to-end (train→predict→gate→eligibility→shadow_diff→endpoints) + TestClient. Remaining A3 exit = the 10 trading-day shadow review (prod/time-gated). | `backend/main.py`, `backend/models.py` |
| 2026-07-05 | — | Infra | **v2 Phase A2 — estimator port + forward-RV forecaster** (silent; v1 untouched). Ported `theta_harvest_core.py` → `backend/theta_core.py` **verbatim** (the 1e-9 oracle; CONFIG home per P3, byte-identical + a golden-master test guards drift) + `estimators.py` (bars→daily_inputs / EWMA replay / YZ21; **recompute-from-bars, no persisted state**) + `forecast.py` (pooled-ridge `ForecastEngine`: total + downside `sigma_fwd`, lagged global factor `G_t`; centered target `ln(RVfwd)−ln(vbar)−ln(ANN)` to invert `predict_sigma_fwd` exactly) + `backfill_v2.py` (train + write per-ticker-date `sigma_fwd`/`sigma_fwd_dn`/`fvrp_ratio`/`fvrp_z`/`accel_dn`/EWMAs/`vbar` onto existing daily_iv rows; **G3-only** bars gate replay — G2 term/G1 earnings need live chain/earnings data). Extended the bars seed to **10y** for genuine multi-regime training. **Result:** forecaster trained on **79,142 obs (2016–2026)**, all 33 tickers warm (≥60 FVRP-z obs); `FVRP_z`≈N(0,1) (sd 0.94); `sigma_fwd` med 0.271. **Finding → Phase-B input:** the forward-VRP denominator (GK+overnight forecast RV) runs materially richer than v1's close-to-close RV → FVRP median **0.92, 68% of ticker-days <1.0** (structural, regime-independent) — the `[PROVISIONAL]` dead zones (1.20/1.15) **require the Phase-B quantile-match** before they gate; T1 adjudicates forward-vs-trailing. Tests: `test_theta_core.py` 13 (1e-9 + friction + CONFIG regressions), `test_estimators.py` 5; v1 green (5+7). | `backend/theta_core.py`, `backend/estimators.py`, `backend/forecast.py`, `backend/backfill_v2.py` |
| 2026-07-05 | — | Infra | **v2 Phase A1 — silent data substrate + historical backfill** (additive; v1 untouched, no UI/behavior change). New SQLite tables `daily_bars`/`index_daily`/`gate_state`/`shadow_diff`/`positions`/`trades`/`portfolio_daily` + 28 additive Module-G columns on `daily_iv` (v2 estimator/forecaster/gate outputs + a `legacy_*` shadow snapshot), via the existing idempotent CREATE-IF-NOT-EXISTS + PRAGMA-guarded ALTER pattern; empty `trial_registry.jsonl`. New v2 writers/readers (`store_daily_bars`/`get_bars`/`get_bars_coverage`/`store_index_bars`/`store_daily_iv_v2`/`get_fvrp_history`/`store_gate_state`/`store_shadow_diff`/`get_shadow_diffs`/`get_shadow_summary`). New `backfill_bars.py`: **yfinance** multi-year OHLC → `daily_bars` (source-flagged) + VIX family → `index_daily`, with bar-integrity/quarantine and `--resume/--period/--dry-run`. **Decision (this session):** yfinance historical seed + MarketData live going-forward (one vbar-absorbed seam) — multi-regime training + resolves the trial-token 1-yr cap. Seeded 33×753 bars (2023-07→2026-07), 0 quarantined, NVDA 10:1 split auto-adjusted; migration idempotent; v1 tests green (5 + 7). | `backend/database.py`, `backend/backfill_bars.py` |
| 2026-07-05 | — | Infra | **v2 Phase 0 — environment & decisions locked** (behavior-preserving). **Deps:** added `pandas>=2.2.2` + `scipy>=1.13.0` to backend requirements — the reference's PSR bias-corrected skew/kurt, `kelly_base` block-bootstrap, and forecaster need them; porting to numpy was rejected to preserve 1e-9 golden-master fidelity (skew/kurt would silently diverge). **CONFIG audit (0.6):** lifted the 5 remaining inline `[PROVISIONAL]` strays into CONFIG with identical values (`fvrp_min_obs` 60, `reentry_ramp_exponent` 2, `margin_otm_min_frac` 0.10, `dial_O_z_clip` 2.0, `psr_min_obs` 30) so **P3**'s "one place" holds fully — 39→44 keys, `py_compile` clean, intrinsic variance/GK math squares left inline. **CLAUDE.md (0.4):** added the P1–P4 hard prohibitions + staged phase arc. **Decisions (in `references/data-sources.md`):** D1 `margin_alpha`=0.20 Reg-T (already in CONFIG), D2 φ=0.25 (already in CONFIG), D3 model-priced T2 first pass, D4 earnings fallback = degrade-single-names-to-index-only-when-stale (scoped) → strategy §3.4 + backend flag ship together in **Phase B**. Endpoint probes 0.1–0.3 staged (need live token; not run — avoids metered spend). Golden master (0.5) reconfirmed. | `backend/requirements.txt`, `references/theta_harvest_core.py`, `CLAUDE.md`, `references/data-sources.md` |
| 2026-07-05 | — | Docs | **v2 phase-plan review fixes** (`tasks/v2-build/`, 7 findings). Fix1: chain-narrowing moved Phase A→E — **verified World A** (single shared `get_options_chain`, `expiration:"all"`, term-slope back tenor = 1Y; narrowing pre-cutover would drop the 1Y tenor and corrupt v1 slope / shadow evidence / automation history). Fix2: eligibility-rate gate replaced with **T1-owned quantile-matching** (dead zones don't move on activity counts). Fix3: `profit_75` made **unconditional** (§5.1); conditional-50% moved to Phase F pending a human ADR; `danger_underwater_mult=1.25` [PROVISIONAL] added to CONFIG + defined in strategy §5.4. Fix4: friction pre-screen **annotate-only until E**. Fix5: **G2 premise corrected** — verified the "~1.00–1.16" band was the inverted (back/front) ratio; empirically index IV_1M/IV_3M is contango (SPY med 0.94, fires 0%; heterogeneous ETF sleeve EEM/XLE/GLD/XLI 42–73%), so per-sleeve re-baseline is the likely outcome; re-baseline mechanism unchanged. Fix6: doc-sync exit criterion (P5) added to all 7 phases. Fix7: D4 same-commit linkage. **No `g2_*` / threshold values changed (P3).** | `tasks/v2-build/*`, `references/strategy_v2.md`, `references/theta_harvest_core.py` |
| 2026-07-04 | — | Feature | **Scoring update (⚠ score-continuity break)** driven by the 16-month backtest (`docs/strategy-backtest-2026-07.md`, converging with the 2026-06 quotes-based study): (1) RV Accel > 1.10 now forces CAUTION on its own (ADR-012) — rising-RV names can no longer print SELL (fixes the NFLX 6/26 false-SELL case); (2) negative-VRP cap raised 44 → 54 (ADR-013, supersedes ADR-004) — still never tradeable via the WATCHLIST ratio gate; (3) `daily_iv` now also persists skew_25d / rv10 / iv_percentile / spot / earnings_dte (auto-migrates on startup) so future backtests are exact instead of imputed. Scores and recommendations shift from this date. | `backend/scorer.py`, `backend/database.py`, `backend/main.py`, `frontend/src/lib/metrics-content.ts` |
| 2026-07-04 | — | Docs | Strategy/metrics accuracy pass synced with the scoring update: measured IV>RV frequency (~70% across the universe vs the ~80% index claim), profit-target guidance 50% → 75%-in-NORMAL / 50%-when-RV-rising, DANGER-exit refined to underwater-only, 25–30Δ friction preference note, THE FINALS never-triggered honesty note (0/284 days), MarketData earnings-fallback 402 reality, WATCHLIST gate documented in every surface it was missing from. | `references/strategy.md`, `references/metrics.md`, `context/1-domain/scoring-and-strategy.md`, `context/3-guardrails/decisions/` |
| 2026-06-05 | — | Infra | History auto-updater **went LIVE** — removed `--shadow` from `com.optionharvest.history.plist` and reloaded the agent (no app rebuild, to preserve the Full Disk Access grant). The 09:00 SGT daily fire now writes the real `history/` files (working tree only — no git; user reviews + pushes). Reverting = re-add `<string>--shadow</string>`. | `automation/launchd/com.optionharvest.history.plist` |
| 2026-06-05 | — | Fix | Backtest self-audit found a **critical reconstruction bug**: offline RV from the gappy quotes-CSV close series inflated RV30 (data gap → "last 30 returns" spans months), flipping VRP sign on ~7% of calibration days (|err|>2 on ~20%) and corrupting the score/VRP/RV-accel slices. Fixed by **joining calibration to the production-recorded signal in `scan_results`** (real `signal_score`/`vrp_ratio`/`term_slope`/`rv_acceleration`) instead of re-deriving it; added a settlement guard (skip expiry-in-data-gap trades, >4d). Headline P/L unaffected (real settlement + real entry quotes). The qualitative finding survived: 65+ SELL bucket still underperforms (57.7% win, −1.04%) on faithful signals; contango still best. | `scripts/naked_put_backtest.py`, `docs/qa/naked-put-backtest-report.md` |
| 2026-06-04 | — | Feature | First empirical validation of the strategy — naked-put **hold-to-expiry backtest** over 15 months. Replays stored option chains through the **real production scorer** (`build_vol_surface`+`score_opportunity`) with `calculator.date.today()` frozen to the as-of date (faithful DTEs, no look-ahead); offline/deterministic (underlying closes + RV from the quotes CSVs, no yfinance). Two fills: **real_atm** (real CSV quotes, trustworthy) + **modeled_20d** (BSM overlay, idealized). 8.7k real-ATM trades. Findings: edge is real but **marginal at effective-N** (PF 2.19→1.34, +0.59%/cycle on 394 independent trades); the **composite score did NOT positively rank outcomes** in this bull/recovery sample (flat-to-inverted calibration, full-chain N=676), while **term-slope (contango>backwardation) and DANGER-worst were directionally validated**. Calibration restricted to full-chain days (backfilled ATM-only 2025 chains give degenerate term/skew). | `scripts/naked_put_backtest.py`, `docs/qa/naked-put-backtest-report.md`, `docs/qa/naked-put-backtest-trades.csv` |
| 2026-06-04 | — | Infra | Renamed the history auto-updater's background item in System Settings → Login Items & Extensions from **"Python Software Foundation"** (the python.org code-sign identity macOS attributed the raw python3 binary to) to **"Theta Harvest"**, by launching the job through a tiny ad-hoc-signed `ThetaHarvest.app` whose Mach-O main executable (`launcher.c`, compiled) is named `Theta Harvest` and just re-execs the same orchestrator. Finding: that pane derives the row from the launched program's signing identity — ad-hoc (free) signing controls only the displayed *name* (the executable filename); a custom **icon** requires a paid Developer ID or SMAppService, so the θ `AppIcon.icns` is pre-built into the bundle but the icon stays generic ("unidentified developer") under ad-hoc. Trade-off: Full Disk Access now applies to the app (responsible process), not python3 — one-time re-grant. `install.sh` builds the app; `build_app.sh` rebuilds standalone. | `automation/launchd/app/Info.plist`, `automation/launchd/app/launcher.c`, `automation/launchd/make_icon.py`, `automation/launchd/build_app.sh`, `automation/launchd/com.optionharvest.history.plist`, `automation/launchd/install.sh` |
| 2026-06-04 | — | Feature | History auto-updater (`automation/`): self-healing daily pipeline that fetches the live scan + backfills missed days (prod SQLite over SSH), reproduces the metrics/CPS tables byte-exact, computes regime/aggregates deterministically, and has headless Claude (Max subscription, no API cost) write the briefing prose + CPS Notable. Capture-before-Claude (zero data loss on auth failure), idempotent, launchd-scheduled, `--shadow`/`--dry-run`/`--no-claude` modes, no git automation. 87 offline checks + live e2e. Phases 1–5; Phase 6 = shadow-validation period. | `automation/render/*`, `automation/sources/*`, `automation/history/*`, `automation/claude/*`, `automation/run_history_update.py`, `automation/launchd/*`, `automation/tests/*` |
| 2026-05-12 | daily-grid-ui-v2 | Feature | Credit Put Spreads MVP (Phases 1–5): tabbed dashboard with Naked Puts / Credit Put Spreads / Journal (Coming Soon); backend `spread_builder` + `regime_overlay` (yfinance VIX/VIX3M/VVIX, UNKNOWN-safe) + `spread_exit_evaluator`; new `/api/credit-put-spreads/latest`; `cps_candidate_history` + `cps_scan_responses` tables; 2-day ticker-level confirmation gating SELL_CPS; SPY/QQQ/IWM only; binary filters + Base-Edge-Score ranking; zero changes to scorer.py / calculator.py; 115/115 tests pass, replay byte-stable. | `backend/config.py`, `backend/spread_builder.py`, `backend/regime_overlay.py`, `backend/spread_exit_evaluator.py`, `backend/main.py`, `backend/models.py`, `backend/database.py`, `frontend/src/components/TabBar.tsx`, `frontend/src/components/CreditPutSpreads*.tsx`, `frontend/src/app/page.tsx`, `references/credit-put-spreads.md`, `references/phase5-credit-put-spreads-completion-report.md` |
| 2026-05-10 | daily-grid-ui-v2 | Refactor | Phase 2C: reframed RV Acceleration on the dashboard as environment-cleanliness signal (Excellent / Good / Acceptable / Caution / Avoid · Wait), removed Full / Half / Quarter sizing prescription from UI + docs, renamed `sizing` → `rvAccelStatus`. Zero scoring / threshold / weight changes; 38 backend tests + replay byte-stable. | `frontend/src/lib/scoring.ts`, `frontend/src/lib/types.ts`, `frontend/src/components/Leaderboard.tsx`, `frontend/src/components/DetailPanel.tsx`, `references/metrics.md`, `references/strategy.md`, `references/phase2c-rv-accel-position-sizing-cleanup-report.md` |
| 2026-04-27 | daily-grid-ui | Feature | Added GitHub-style 2026 activity grid for daily Avg VRP inside RegimeBanner; new `/api/vrp-history` endpoint aggregates `daily_iv` rows by date; sage palette ramp (5 steps) added to both themes | `backend/main.py`, `backend/database.py`, `frontend/src/components/VrpActivityGrid.tsx`, `frontend/src/components/RegimeBanner.tsx`, `frontend/src/app/globals.css` |
| 2026-04-27 | daily-grid-ui | Fix | Activity grid data audit: deleted 27 spurious rows for 2026-02-14 (Saturday — backfill artifact); backfilled missing 2026-04-15 (33 rows from markdown); added `HAVING COUNT(*) >= 30` floor in `get_vrp_history_by_date` so partial-scan dates (e.g. 2026-02-12 with 18 tickers) no longer silently disagree with the "33-ticker mean" caption | `backend/database.py`, `backend/data/vol_history.db` |
| 2026-04-27 | daily-grid-ui | Data | Backfilled local `daily_iv` for 2026-04-17 → 04-24 (196 rows, 6 dates) from `history/metrics-logs.md` via new `import_metrics_log.py` importer; production-Lightsail data now available locally for the activity grid | `backend/import_metrics_log.py`, `backend/data/vol_history.db` |
| 2026-04-27 | daily-briefing | Docs | Updated deployment.md with AWS Lightsail production section; documented `history/` folder in CLAUDE.md and start-here.md for agent onboarding; updated freshness dates | `context/2-system/deployment.md`, `context/start-here.md`, `CLAUDE.md` |
| 2026-04-24 | daily-briefing | Data | Daily scan analysis sessions for Apr 20–24; logged raw metrics and briefings with regime assessments, trade recommendations, and position tracking | `history/metrics-logs.md`, `history/daily-briefings.md` |
| 2026-04-24 | daily-briefing | Infra | Merged AWS Lightsail deployment (PR #12); production moved from local MacBook to Lightsail instance in us-east-1 | `prompts/deploy-lightsail.md`, `docker-compose.yml` |
| 2026-04-16 | v1.20 | Docs | Restructured `/context/` into tiered subdirectories (`1-domain/`, `2-system/`, `3-guardrails/`); rewrote `start-here.md` as agent onboarding entry point with reading order | `context/start-here.md`, `CLAUDE.md` |
| 2026-04-16 | v1.20 | Docs | Updated CLAUDE.md with accurate endpoint table, test commands, ticker count, git policy, and agent onboarding instructions | `CLAUDE.md` |
| 2026-04-16 | v1.15 | Docs | Built initial `/context/` folder — glossary, methodology, scoring-and-strategy, architecture, data-model, deployment, fragile-seams, 11 ADRs | `context/` (all files) |
| 2026-04-15 | v1.15 | Fix | Added vega normalization heuristic — MarketData.app intermittently returns raw BSM vega (~100x larger); divide by 100 when \|vega\| > 5 | `backend/calculator.py` |
| 2026-04-02 | v1.12 | Feature | Added strategy summary and OpenBB data exploration | `references/strategy.md` |
| 2026-03-27 | v1.11 | Feature | UI improvements — dark/light theme, ticker drawer, score breakdown, keyboard navigation | `frontend/src/components/`, `frontend/src/hooks/`, `globals.css` |
| 2026-03-25 | v1.08 | Feature | Day-over-day scan comparison; shows delta badges for score, IV rank, VRP changes | `backend/main.py`, `backend/database.py`, `frontend/src/app/page.tsx` |
| 2026-03-25 | v1.08 | Fix | Skew expiration alignment (recurrence) — switched from `dte=` to `expiration=YYYY-MM-DD` to match API exactly | `backend/marketdata_client.py` |
| 2026-03-25 | v1.08 | Fix | CSV date bug — `append_daily_csv()` used stale bar date instead of current ET trading date | `backend/csv_store.py` |
| 2026-03-25 | v1.08 | Refactor | Replaced penalty-based scoring with additive model (VRP 0-30, IV Pct 0-25, Term 0-20, RV 0-15, Skew 0-10) | `backend/scorer.py`, `frontend/src/lib/scoring.ts` |
| 2026-03-21 | v1.07 | Fix | Skew expiration alignment — wide chain hardcoded `dte=30`, causing most tickers to have zero skew | `backend/marketdata_client.py`, `backend/calculator.py` |
| 2026-03-13 | v1.05 | Fix | Bug fixes for scoring and data pipeline stability | `backend/` |
| 2026-02-22 | v1.03 | Feature | Yahoo Finance earnings verification — cross-checks FMP dates, auto-overrides on >5-day discrepancy | `backend/main.py`, `utils/verify_metrics.py` |
| 2026-02-18 | v1.02 | Feature | Frontend upgrades — leaderboard UI, regime banner, detail panel | `frontend/src/components/`, `frontend/src/app/page.tsx` |
| 2026-02-14 | v1.00 | Infra | Cloudflare tunnel deployment, PC hosting setup, Docker compose stack | `docker-compose.yml`, `backend/Dockerfile`, `frontend/Dockerfile` |
| 2026-02-14 | v1.00 | Feature | Initial release — full scan pipeline, scoring engine, SQLite persistence, Next.js dashboard | `backend/`, `frontend/` |
